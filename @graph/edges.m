function eg = edges(g)
% EDGES - return edges
%
% Example:
% edges = edges(g);
%
% This function directly edges or link list of the graph as it was stored.
% It should be noted that if the graph is set to undirected, the edges is
% not updated, although it reflect on adjacency matrix.

eg = g.edges;